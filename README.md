# Solar System Simulation

This project is a simple simulation of the solar system. It visualizes the planets and their orbits around the Sun.

## Portfolio

Check out my portfolio site: [Sahil Bhoite's Portfolio](https://sahil-bhoite.github.io/Website/)

## Repository

Once the README is ready, push this repository to GitHub:

1. Initialize a new Git repository (if not already initialized):
   ```bash
   git init
   ```

2. Add the remote repository:
   ```bash
   git remote add origin https://github.com/Sahil-Bhoite/Space-
   ```

3. Add all files and commit:
   ```bash
   git add .
   git commit -m "Initial commit with solar system simulation"
   ```

4. Push to the GitHub repository:
   ```bash
   git push -u origin main
